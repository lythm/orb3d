


float4 PS( float4 Pos : SV_POSITION ) : SV_Target
{
	//return float4( Pos.x, Pos.y, Pos.z, 1.0 );    // Yellow, with Alpha = 1

	//return float4(Pos.x + 0.5, 0, 0, 1.0);
	
	Pos.x = Pos.x / 1024;
	Pos.y = Pos.y / 768;
	Pos.z = 0.5;
	Pos.w = 1;
	return Pos;
}

