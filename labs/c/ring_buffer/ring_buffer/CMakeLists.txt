 
PROJECT(ring_buffer)
CMAKE_MINIMUM_REQUIRED(VERSION 2.8)

AUX_SOURCE_DIRECTORY(./ DIR_SRCS)

INCLUDE_DIRECTORIES(./)

ADD_EXECUTABLE(ring_buffer ${DIR_SRCS})
SET_TARGET_PROPERTIES(ring_buffer PROPERTIES RUNTIME_OUTPUT_DIRECTORY ./)
